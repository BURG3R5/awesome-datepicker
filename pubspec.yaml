name: awesome_datepicker
description: A Flutter widget where you select a date using a color picker
version: 0.0.1
homepage: https://adityarajput.co/awesome_datepicker

environment:
  sdk: '>=2.19.1 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
